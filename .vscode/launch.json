{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Uruchom aktualny plik",
            "skipFiles": ["<node_internals>/**"],
            "program": "${file}"
        }
    ]
}
